<!DOCTYPE html>
<html>
<head>
  <title>SchoolBridge</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "tailwind", "custom", media: "all", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", defer: true, "data-turbo-track": "reload" %>
</head>

<body class="devise-page">
<header class="devise-header" style="position:relative; z-index:10;
  display:flex; justify-content:space-between; align-items:center;
  padding:1rem 2rem; background:white;">
  <div style="display:flex; align-items:center;">
    <%= image_tag "logo.svg", class:"devise-logo", style:"width:48px; height:auto; margin-right:1rem;" %>
    <div>
      <small style="display:block; color:#b77a07; letter-spacing:0.05em;
        font-family:'Bahnschrift',sans-serif; font-size:0.75rem;">
        FOREIGN LANGUAGE SCHOOL “PROF DR ASSEN ZLATAROV”
      </small>
      <h1 style="margin:0; font-family:'Bahnschrift',sans-serif; font-size:1.5rem;
        color:#2a1500; letter-spacing:0.05em;">
        SCHOOLBRIDGE
      </h1>
    </div>
  </div>

  <nav>
    <%= link_to "Sign in", new_person_session_path,
                class:("text-[#2a1500] font-medium hover:underline" if controller_name=="sessions") %>
    |
    <%= link_to "Register", new_person_registration_path,
                class:"text-[#2a1500] font-medium hover:underline" %>
  </nav>
</header>


<div class="devise-bg" style="
position:fixed; top:0; left:0; right:0; bottom:0;
  background: linear-gradient(to bottom, #8c3f00 0%, #5e2c00 100%);
  z-index:0;">
</div>


<div style="
position:relative; z-index:5;
  display:flex; justify-content:center; align-items:center;
  min-height:calc(100vh - 80px - 40px); /* header + footer space */
  padding:2rem;">
  <div style="
  background:white;
    border-radius:1rem;
    box-shadow:0 4px 20px rgba(0,0,0,0.1);
    max-width:400px;
    width:100%;
    text-align:center;
    padding:2rem;">
    <h2 style="
    margin-bottom:1.5rem;
      font-family:'Bahnschrift',sans-serif;
      font-size:2rem;
      color:#6e4200;">
      Sign in
    </h2>
    <%= yield %>
  </div>
</div>

<footer style="
position:fixed; bottom:0; left:0; right:0;
  background:white; text-align:center; padding:1rem;
  font-size:0.8rem; font-family:'Bahnschrift',sans-serif;">
  ©2025 SchoolBridge – K2336620
</footer>
</body>
</html>
